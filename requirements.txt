# FinAgent-RAG pinned dependencies.
#
# Versions reflect what was actually verified working during implementation (see
# CLAUDE.md "Requirement Deviations" for the two intentional deviations from the proposal's
# Table 8.1 minimum versions: numpy and scikit-learn).

# --- Explicitly named in Proposal Table 8.1 ---
langchain==0.3.27
chromadb==0.5.0
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.5.0
faiss-cpu==1.12.0

# --- Required for implementation but not named in the proposal's condensed requirements table ---
groq==0.30.0                 # Groq API SDK (Llama 3.3 70B Versatile client)
sentence-transformers==2.6.1 # all-MiniLM-L6-v2 embedding backend for ChromaDB
tiktoken==0.11.0             # token counting for chunk sizing (cl100k_base proxy)
tenacity==9.1.2              # bounded exponential-backoff retry for Groq calls
python-dotenv==1.1.1         # GROQ_API_KEY loading from .env
pypdf==6.0.0                 # PDF text extraction (no poppler/pdftoppm on this machine)
openpyxl==3.1.5              # Coding_Sheet_RESULTS.xlsx read/write

# Transitive pins for reproducibility (installed automatically by the above, pinned explicitly
# so `pip install -r requirements.txt` reproduces this exact working environment).
transformers==4.56.2
torch==2.5.1
